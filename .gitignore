# Python virtual environment (recreate with pip install, don't version)
.venv/
__pycache__/
*.pyc
*.egg-info/

# Subagent-driven-development scratch (briefs, reports, progress ledger)
.superpowers/

# Local Hugging Face model cache (HF_HOME points here; ~2 GB, re-downloadable)
huggingface/

# Installer build artifacts (installer/build.ps1 outputs)
installer/payload/
installer/cache/
installer/Output/
installer/tmp-smoke/

# Large data / generated outputs live next to the VOD in E:\Videos\..., not here.
# If you ever copy them in, keep these out of git:
*.mp4
*.onnx
motion.csv
ocr.csv
